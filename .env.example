# Core settings for Django
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings (SQLite para desenvolvimento, PostgreSQL para produção)
DATABASE_URL=sqlite:///db.sqlite3
# Para PostgreSQL em produção: postgresql://user:password@localhost:5432/oportunidades_belem

# Email settings (opcional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# WhatsApp configuration
WHATSAPP_DEFAULT_DDD=91

# Futturu branding
FUTTURL_LINK=https://futturu.com.br
